const INCOMING = "incoming";
const OUTGOING = "outgoing";
const ACCEPTED = "accepted";
const ACCEPT = "accept";
const JOIN = "join";

export {
    INCOMING,
    ACCEPT,
    ACCEPTED,
    OUTGOING,
    JOIN
}