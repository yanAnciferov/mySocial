const INCOMING = "incoming";
const OUTGOING = "outgoing";
const ACCEPTED = "accepted";
const ACCEPT = "accept";
const JOIN = "join";
const REMOVE = "remove";
const REJECT = "reject";

export {
    INCOMING,
    ACCEPT,
    ACCEPTED,
    OUTGOING,
    JOIN,
    REMOVE,
    REJECT
}